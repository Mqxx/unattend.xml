<component
  name="Microsoft-Windows-Deployment"
  processorArchitecture="amd64"
  publicKeyToken="31bf3856ad364e35"
  language="neutral"
  versionScope="nonSxS"
  xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
>
  <!-- Configures system deployment settings in audit mode, such as reseal options -->
  <!-- Configures deployment settings during the auditSystem phase -->
  <Reseal>
    <!-- Configures settings for resealing the system in audit mode -->
    <ForceShutdownNow>false</ForceShutdownNow>
    <!-- Specifies whether to force an immediate shutdown after resealing -->
    <Mode>Audit</Mode>
    <!-- Specifies the mode to enter after resealing (Audit or OOBE) -->
  </Reseal>
</component>
